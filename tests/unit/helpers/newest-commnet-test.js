import { newestCommnet } from 'question-answer/helpers/newest-commnet';
import { module, test } from 'qunit';

module('Unit | Helper | newest commnet');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = newestCommnet([42]);
  assert.ok(result);
});
